# Build Notes

This file exists to document release-build notes and can also be used to safely trigger the Android build workflow without changing app logic.

Latest manual trigger: finalize GMD Uploader v1 polish build.
